version: '3'
services:
  comms-server:
    image: katalyst:latest
    working_dir: /app
    command: comms
    ports:
      - "9000:9000"
  content-server:
    image: katalyst:latest
    working_dir: /app
    command: content
    ports:
      - "${CONTENT_SERVER_PORT}:6969"
  lambdas:
    image: katalyst:latest
    working_dir: /app
    command: lambdas
    ports:
      - "7070:7070"
