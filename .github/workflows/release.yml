name: release
on: [workflow_dispatch]
jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            console.log(context.payload.repository.tags_url)
