name: validate-pr-title
on:
  pull_request:
    types: [edited, opened, reopened, synchronize]
jobs:
  title-matches-convention:
    runs-on: ubuntu-latest
    steps:
      - uses: decentraland/actions/.github/workflows/validate-pr-title.yml@chore/pr-title-validation
