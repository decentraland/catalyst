name: build

on:
  push:
  pull_request:

jobs:
  build:
    uses: decentraland/platform-actions/.github/workflows/apps-with-db-build.yml@db
    with:
      postgres-user: postgres
      postgres-password: 12345678
      postgres-db: postgres_test
